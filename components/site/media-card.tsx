import Image from "next/image";import Link from "next/link";import {ArrowUpRight} from "lucide-react";import {UnescoBadge} from "@/components/destinations/unesco-badge";import {Badge} from "@/components/ui/badge";
export function MediaCard({href,image,alt,title,eyebrow,description,tall=false,unesco=false}:{href:string;image:string|null;alt:string|null;title:string;eyebrow?:string|null;description?:string|null;tall?:boolean;unesco?:boolean}){return <Link href={href} className="group focus-ring block"><article className="overflow-hidden rounded-lg border border-divider bg-surface"><div className={`relative overflow-hidden bg-sand ${tall?"aspect-[4/5]":"aspect-[4/3]"}`}>{image?<Image src={image} alt={alt||title} fill sizes="(max-width: 768px) 100vw, 33vw" className="object-cover transition duration-300 motion-safe:group-hover:scale-[1.025]"/>:<div className="absolute inset-0 grid place-items-center text-sm text-muted">Image awaiting review</div>}{eyebrow&&<Badge className="absolute left-4 top-4">{eyebrow}</Badge>}{unesco&&<UnescoBadge className="absolute bottom-4 left-4"/>}</div><div className="p-5"><div className="flex items-start justify-between gap-4"><h3 className="font-serif text-2xl leading-tight">{title}</h3><ArrowUpRight aria-hidden="true" className="mt-1 size-5 shrink-0 text-forest transition-transform duration-180 motion-safe:group-hover:translate-x-1 motion-safe:group-hover:-translate-y-1"/></div>{description&&<p className="mt-3 line-clamp-2 text-sm leading-6 text-muted">{description}</p>}</div></article></Link>}
