import {MediaCard} from "./media-card";import {SectionHeading} from "./section-heading";
type Item={id:string;slug:string;name:string;image:string|null;alt:string|null;eyebrow?:string|null;description?:string|null;unesco?:boolean};
export function ListingPage({eyebrow,title,copy,basePath,items}:{eyebrow:string;title:string;copy:string;basePath:string;items:Item[]}){return <><section className="bg-sand-light py-24 md:py-32"><div className="shell"><SectionHeading eyebrow={eyebrow} title={title} copy={copy}/></div></section><section className="section"><div className="shell">{items.length?<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map(item=><MediaCard key={item.id} href={`${basePath}/${item.slug}`} image={item.image} alt={item.alt} title={item.name} eyebrow={item.eyebrow} description={item.description} unesco={item.unesco}/>)}</div>:<div className="rounded-3xl border border-dashed border-stone/40 p-16 text-center text-stone">Reviewed listings will appear here soon.</div>}</div></section></>}
