"use client";
import Image from "next/image";import Link from "next/link";import {AnimatePresence,motion} from "motion/react";import {Check,ChevronLeft,ChevronRight,MapPin,Sparkles} from "lucide-react";import {useMemo,useState} from "react";
import {Button} from "@/components/ui/button";import {cn} from "@/lib/utils";import type {JourneyBootstrap} from "@/lib/journey/journey-service";import {availableDestinations,availableExperiences,availableStays} from "@/lib/journey/journey-selectors";import {JourneyProvider,useJourney} from "./journey-store";import {SriLankaMap} from "@/components/map/sri-lanka-map";
import {useHydrated} from "@/lib/hooks/use-hydrated";
const steps=["Theme","Destination","Experience","Plan"] as const;
type Card={id:string;name:string;hero_image_url:string|null;short_description:string|null;category?:string|null};

function Builder({data}:{data:JourneyBootstrap}){
  const {state,dispatch}=useJourney(),[step,setStep]=useState(0),[planTab,setPlanTab]=useState("Stay");
  const destinations=useMemo(()=>availableDestinations(data.destinations,state.selectedThemeIds),[data.destinations,state.selectedThemeIds]);
  const experiences=useMemo(()=>availableExperiences(data.experiences,state.selectedDestinationIds),[data.experiences,state.selectedDestinationIds]);
  const stays=useMemo(()=>availableStays(data.stays,state.selectedDestinationIds),[data.stays,state.selectedDestinationIds]);
  const vehicles=data.vehicles.filter(x=>x.nationwide||x.destinationIds.some(id=>state.selectedDestinationIds.includes(id)));
  const guides=data.guides.map(x=>({...x,relevance:x.destinationIds.filter(id=>state.selectedDestinationIds.includes(id)).length*4+x.themeIds.filter(id=>state.selectedThemeIds.includes(id)).length*2+x.experienceIds.filter(id=>state.selectedExperienceIds.includes(id)).length*3+(x.verified?1:0)})).filter(x=>x.nationwide||x.relevance>0).sort((a,b)=>b.relevance-a.relevance);
  const current=[data.themes,destinations,experiences][step] as Card[]|undefined;
  const selected=[state.selectedThemeIds,state.selectedDestinationIds,state.selectedExperienceIds][step]||[];
  const fields=["selectedThemeIds","selectedDestinationIds","selectedExperienceIds"] as const;
  const card=(item:Card)=><button key={item.id} onClick={()=>dispatch({type:"toggle",field:fields[step],id:item.id})} className={cn("group overflow-hidden rounded-3xl border bg-white text-left transition",selected.includes(item.id)?"border-gold ring-2 ring-gold/25":"border-stone/15 hover:-translate-y-1")}><div className="relative aspect-[16/9] bg-sand">{item.hero_image_url&&<Image src={item.hero_image_url} alt={item.name} fill sizes="50vw" className="object-cover transition duration-700 group-hover:scale-105"/>}<span className={cn("absolute right-4 top-4 grid size-8 place-items-center rounded-full border backdrop-blur",selected.includes(item.id)?"border-gold bg-gold text-white":"border-white/50 bg-slate/30 text-white")}>{selected.includes(item.id)&&<Check className="size-4"/>}</span></div><div className="p-5">{item.category&&<p className="text-xs font-bold uppercase tracking-widest text-gold">{item.category}</p>}<h2 className="mt-2 font-serif text-2xl">{item.name}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate/60">{item.short_description}</p></div></button>;
  return <div className="shell grid gap-8 py-12 lg:grid-cols-[1fr_340px]"><section><div className="mb-10 flex gap-2 overflow-x-auto">{steps.map((label,i)=><button key={label} onClick={()=>i<=step&&setStep(i)} className={cn("flex min-w-fit items-center gap-2 rounded-full px-4 py-2 text-xs font-bold",i===step?"bg-forest text-ivory":i<step?"bg-sand text-forest":"bg-stone/10 text-stone")}>{i<step?<Check className="size-3"/>:i+1} {label}</button>)}</div><AnimatePresence mode="wait"><motion.div key={step} initial={{opacity:0,x:22}} animate={{opacity:1,x:0}} exit={{opacity:0,x:-18}}><p className="eyebrow mb-3">Step {step+1} of 4</p><h1 className="font-serif text-4xl md:text-6xl">{["What kind of journey draws you in?","Where would you like to wake up?","Choose the moments that matter.","Complete your journey."][step]}</h1><p className="mt-4 max-w-2xl text-slate/60">{step===1&&!state.selectedThemeIds.length?"Choose a theme to reveal its linked destinations.":step===2&&!state.selectedDestinationIds.length?"Choose a destination to reveal its linked experiences.":"Your choices are saved on this device as you build."}</p>{step<3?<><div className="mt-10 grid gap-5 md:grid-cols-2">{current?.map(card)}</div>{step===1&&<div className="mt-10"><SriLankaMap destinations={destinations} selectedIds={state.selectedDestinationIds} onSelect={id=>dispatch({type:"toggle",field:"selectedDestinationIds",id})}/></div>}</>:<div className="mt-10"><div className="flex gap-2 rounded-2xl bg-sand-light p-2">{["Stay","Getting Around","Local Guides"].map(tab=><button onClick={()=>setPlanTab(tab)} key={tab} className={cn("flex-1 rounded-xl px-3 py-3 text-sm font-bold",tab===planTab?"bg-white shadow-sm":"text-slate/50")}>{tab}</button>)}</div><div className="mt-5 grid gap-4">{planTab==="Stay"?(stays.length?stays.map(item=><button key={item.id} onClick={()=>dispatch({type:"stay",destinationId:item.destinationId,stayId:item.id})} className="rounded-2xl border border-stone/20 bg-white p-5 text-left"><strong>{item.name}</strong><span className="block text-sm text-stone">{item.property_type}</span></button>):<Empty text="No published stays are available for your selected destinations yet."/>):planTab==="Getting Around"?(vehicles.length?vehicles.map(item=><button key={item.id} onClick={()=>dispatch({type:"vehicle",id:item.id})} className="rounded-2xl border border-stone/20 bg-white p-5 text-left"><strong>{item.listing_title}</strong><span className="block text-sm text-stone">{item.vehicle_type} · {item.passenger_capacity||"Flexible"} guests</span></button>):<Empty text="Vehicle partners are being added. Submit your journey and we will help arrange transport."/>):(guides.length?guides.map(item=><button key={item.id} onClick={()=>dispatch({type:"guide",id:item.id})} className="rounded-2xl border border-stone/20 bg-white p-5 text-left"><strong>{item.name}</strong><span className="block text-sm text-stone">{item.languages.join(" · ")}</span></button>):<Empty text="No matching local guides are currently published."/>)}</div></div>}<div className="mt-10 flex justify-between"><Button variant="ghost" disabled={step===0} onClick={()=>setStep(x=>x-1)}><ChevronLeft/>Back</Button>{step<3?<Button onClick={()=>setStep(x=>x+1)}>Continue<ChevronRight/></Button>:<Button asChild variant="accent"><Link href="/contact">Send to a journey designer</Link></Button>}</div></motion.div></AnimatePresence></section><Summary data={data}/></div>
}
function Empty({text}:{text:string}){return <div className="rounded-2xl border border-dashed border-stone/30 p-8 text-sm text-stone">{text}</div>}
function Summary({data}:{data:JourneyBootstrap}){const {state}=useJourney();return <aside className="h-fit rounded-3xl bg-forest p-7 text-ivory lg:sticky lg:top-28"><div className="mb-6 flex items-center gap-3"><Sparkles className="text-gold-light"/><h2 className="font-serif text-2xl">Your journey</h2></div>{[[state.selectedThemeIds,data.themes,"Themes"],[state.selectedDestinationIds,data.destinations,"Destinations"],[state.selectedExperienceIds,data.experiences,"Experiences"]].map(([ids,items,label])=><div className="border-t border-ivory/10 py-5" key={label as string}><p className="mb-3 text-[.65rem] font-bold uppercase tracking-widest text-gold-light">{label as string}</p><div className="flex flex-wrap gap-2">{(ids as string[]).length?(ids as string[]).map(id=><span key={id} className="rounded-full bg-ivory/10 px-3 py-1 text-xs">{(items as Card[]).find(x=>x.id===id)?.name}</span>):<span className="text-sm text-ivory/40">Nothing selected yet</span>}</div></div>)}<div className="mt-4 flex items-center gap-2 text-xs text-ivory/55"><MapPin className="size-4"/>Selections persist after refresh.</div></aside>}
export function JourneyBuilder({data}:{data:JourneyBootstrap}){
  const hydrated=useHydrated();
  if(!hydrated)return <div className="shell grid gap-8 py-12 lg:grid-cols-[1fr_340px]" aria-label="Loading saved journey"><div className="h-[38rem] animate-pulse rounded-3xl bg-sand-light"/><div className="h-80 animate-pulse rounded-3xl bg-forest/90"/></div>;
  return <JourneyProvider data={data}><Builder data={data}/></JourneyProvider>;
}
