"use client";
import {useRouter} from "next/navigation";import {useState} from "react";import {Button} from "@/components/ui/button";import {createClient} from "@/lib/supabase/client";
export function LoginForm(){const router=useRouter(),[error,setError]=useState(""),[loading,setLoading]=useState(false);return <form className="grid gap-5" onSubmit={async e=>{e.preventDefault();setLoading(true);setError("");const form=new FormData(e.currentTarget);const {error}=await createClient().auth.signInWithPassword({email:String(form.get("email")),password:String(form.get("password"))});if(error){setError("We could not sign you in with those details.");setLoading(false)}else{router.push("/admin/dashboard");router.refresh()}}}><label className="grid gap-2 text-sm font-semibold">Email<input name="email" type="email" required className="rounded-xl border border-stone/30 px-4 py-3"/></label><label className="grid gap-2 text-sm font-semibold">Password<input name="password" type="password" required className="rounded-xl border border-stone/30 px-4 py-3"/></label>{error&&<p className="text-sm text-red-700">{error}</p>}<Button disabled={loading}>{loading?"Signing in…":"Sign in"}</Button></form>}
