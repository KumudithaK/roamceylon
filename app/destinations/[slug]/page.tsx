import type {Metadata} from "next";import {notFound} from "next/navigation";import {DetailPage} from "@/components/site/detail-page";import {getBySlug} from "@/lib/data";import type {Destination} from "@/lib/types";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const item=await getBySlug("destinations",slug) as Destination|null;return item?{title:item.name,description:item.short_description,alternates:{canonical:`/destinations/${slug}`}}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const item=await getBySlug("destinations",(await params).slug) as Destination|null;if(!item)notFound();return <DetailPage backHref="/destinations" backLabel="All destinations" title={item.name} eyebrow={item.province} image={item.hero_image_url} alt={item.image_alt} description={item.short_description} fullDescription={item.full_description} gallery={item.gallery} metadata={[["Region",item.region||item.province],["Location",item.latitude&&item.longitude?`${item.latitude}, ${item.longitude}`:null]]}/>}
