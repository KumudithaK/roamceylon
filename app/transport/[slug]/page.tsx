import {notFound} from "next/navigation";import {DetailPage} from "@/components/site/detail-page";import {getBySlug} from "@/lib/data";import type {Vehicle} from "@/lib/types";
export default async function Page({params}:{params:Promise<{slug:string}>}){const item=await getBySlug("vehicles",(await params).slug) as Vehicle|null;if(!item)notFound();return <DetailPage backHref="/transport" backLabel="All transport" title={item.listing_title} eyebrow={item.vehicle_type} image={item.hero_image_url} alt={item.image_alt} description={item.short_description} gallery={item.gallery} metadata={[["Model",item.vehicle_model],["Passenger capacity",item.passenger_capacity?`${item.passenger_capacity} guests`:null],["Luggage capacity",item.luggage_capacity],["Air conditioning",item.air_conditioned?"Yes":"No"],["Driver included",item.driver_included?"Yes":"No"]]}/>}
