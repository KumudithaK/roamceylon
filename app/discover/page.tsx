import {listContent} from "@/lib/data";import {ListingPage} from "@/components/site/listing-page";import {editionDisplayName} from "@/lib/brand";
export default async function Page(){const items=await listContent("themes");return <ListingPage eyebrow="Discover your Sri Lanka" title="Choose your Edition." copy="Explore the island through wildlife, heritage, coast, culture, nature, wellness, adventure and sport." basePath="/discover" items={items.map(x=>({id:x.id,slug:x.slug,name:editionDisplayName(x),image:x.hero_image_url,alt:x.image_alt,eyebrow:"Edition",description:x.short_description}))}/>}
