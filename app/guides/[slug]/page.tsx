import {notFound} from "next/navigation";import {DetailPage} from "@/components/site/detail-page";import {getBySlug} from "@/lib/data";import type {Guide} from "@/lib/types";
export default async function Page({params}:{params:Promise<{slug:string}>}){const item=await getBySlug("guides",(await params).slug) as Guide|null;if(!item)notFound();return <DetailPage backHref="/guides" backLabel="All guides" title={item.name} eyebrow="Local guide" image={item.profile_image_url} alt={item.image_alt} description={item.short_bio} fullDescription={item.full_bio} gallery={item.gallery} metadata={[["Languages",item.languages.join(" · ")],["Experience",item.years_experience?`${item.years_experience} years`:null],["Specialities",item.specialities.join(" · ")]]}/>}
